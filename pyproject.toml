[project]
name = "study-space-code"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { email = "nguyencanhnguyen159357@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "gymnasium[classic-control]>=1.3.0",
    "scikit-learn>=1.9.0",
    "stable-baselines3>=2.9.0",
    "torch>=2.13.0",
]

[project.scripts]
study-space-code = "study_space_code:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
